## Live Redshift validation evidence

- Commit SHA:
- UTC date and time:
- Operator:
- AWS account suffix only:
- Region:
- Django version:
- redshift-connector version:
- Endpoint (sanitized):
- Accepted CIDR:

## Checks selected for this session

Record the exact commands or probes and their sanitized outcomes. There is no
fixed validation command.

- Command/probe:
  - Result:
  - Cleanup performed:

## Infrastructure cleanup

- [ ] `cdk destroy` succeeded.
- [ ] The CloudFormation validation stack is absent.
- [ ] The workgroup is absent.
- [ ] The namespace is absent.
- [ ] No manual or final snapshot remains.
- [ ] The local `examples/cdk/cdk.out/` assembly was removed.
- [ ] `DB_PASSWORD` and `DATABASE_URL` were cleared.

Do not paste passwords, access keys, session tokens, bearer tokens, full AWS
account IDs, or unsanitized connection URLs into this record.
