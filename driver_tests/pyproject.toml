[project]
name = "django-redshift-backend-driver-investigation"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "packaging>=24",
    "pytest>=8,<9",
    "redshift-connector>=2.1.14,<3",
]
